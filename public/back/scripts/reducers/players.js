import {INIT_PLAYER_LIST} from '../constants/playerTypes'

import reducerHandlerBuild from '../../../common/reducerHandlerBuild'
const handler = {
  [INIT_PLAYER_LIST](state, action) {

    return state;
  }
};

export default reducerHandlerBuild(handler,[]);
